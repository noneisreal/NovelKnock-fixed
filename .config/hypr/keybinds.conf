########################## Programs ############################
#for maintain and config so i will make shotcuts in here :<< it's ok? (noneisreal)
$terminal = kitty
$fileManager = dolphin
$mainmod = SUPER
$Browser = firefox
########################## Language ############################
#i don't use ibus so i will comment all this if you use jus uncomment :<< (noneisreal)

#bind = $mainmod shift, V, exec, ~/.config/eww/scripts/language --switch '{"name":"Vietnamese","name_abbr":"VIE","name_ibus":"Bamboo::Us","name_method":"Vietnamese Telex (Bamboo Engine)"}' &
#bind = $mainmod shift, E, exec, ~/.config/eww/scripts/language --switch '{"name":"English (United States)","name_abbr":"ENG","name_ibus":"xkb:us::eng","name_method":"US"}' &
#bind = $mainmod, Space, exec, ~/.config/eww/scripts/language --cycle &
#################### It just works™ keybinds ###################
# Volume
bindl = ,XF86AudioMute, exec, pactl set-sink-mute @DEFAULT_SINK@ toggle
bindle=, XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
bindle=, XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindle = , XF86AudioRaiseVolume, exec, ~/.config/eww/scripts/volume osd &
bindle = , XF86AudioLowerVolume, exec, ~/.config/eww/scripts/volume osd &
bindl = , XF86AudioMute, exec, ~/.config/eww/scripts/volume osd &

# Brightness
bindle=, XF86MonBrightnessUp, exec, light -A 3 && ~/.config/eww/scripts/brightness osd &
bindle=, XF86MonBrightnessDown, exec, light -U 3 && ~/.config/eww/scripts/brightness osd &

######################################## Keybinds ########################################
# Apps: just normal apps
bind = $mainmod, e, exec, $fileManager
bind = $mainmod, c, exec, code --password-store=gnome --enable-features=UseOzonePlatform --ozone-platform=wayland
bind = control+shift+alt, y, exec, yuzu
bind = $mainmod, t, exec, foot --override shell=fish
bind = $mainmod shift alt, T, exec, foot sleep 0.01 && nmtui
bind = $mainmod, t, exec, $terminal
bind = $mainmod,return, exec, $terminal
bind = $mainmod, e, exec, nautilus --new-window
bind = $mainmod, w, exec, $Brower
bind = $mainmod alt, e, exec, /usr/bin/microsoft-edge-stable --password-store=gnome --enable-features=UseOzonePlatform --ozone-platform=wayland --gtk-version=4
bind = $mainmod, x, exec, gnome-text-editor --new-window
bind = $mainmod shift, w, exec, wps

# Apps: Settings and config
bind = $mainmod, i, exec, XDG_CURRENT_DESKTOP="gnome" gnome-control-center
bind = control $mainmod, v, exec, pavucontrol
bind = control+shift, Escape, exec, gnome-system-monitor
bind =control+alt+shift, Escape, exec, foot -T 'btop' btop
bind = $mainmod shift, i, exec, ~/.local/bin/guifetch

# Actions 
bind = $mainmod shift, Period, exec, pkill wofi || wofi-emoji
bind = $mainmod, Period, exec, pkill fuzzel || ~/.local/bin/fuzzel-emoji
bind = $mainmod, q, killactive,
bind = $mainmod alt, Space, togglefloating,
bind = shift $mainmod alt, Q, exec, hyprctl kill
bind = control+alt, Delete, exec, pkill wlogout || wlogout -p layer-shell
bind = control+shift+alt $mainmod, Delete, exec, systemctl poweroff

# Screenshot, Record, OCR (Optical Character Recognition), Color picker, Clipboard history
bind = $mainmod shift, d, exec,~/.local/bin/rubyshot | wl-copy
bind = $mainmod shift+alt, s, exec, grim -g "$(slurp)" - | swappy -f -
bindl =,Print,exec,grim - | wl-copy
bind = $mainmod shift, s, exec, grim -g "$(slurp)" - | wl-copy
bind = $mainmod alt, r, exec, ~/.local/bin/record-script.sh
bind = control+alt, r, exec, ~/.local/bin/record-script.sh --sound
bind = $mainmod shift+alt, r, exec, ~/.local/bin/record-script-fullscreen.sh
bind = control $mainmod shift, s,exec,grim -g "$(slurp)" "tmp.png" && tesseract "tmp.png" - | wl-copy && rm "tmp.png"
bind = $mainmod shift, t,exec,grim -g "$(slurp)" "tmp.png" && tesseract "tmp.png" - | wl-copy && rm "tmp.png"
bind = $mainmod shift, c, exec, hyprpicker -a
bind = $mainmod, b, exec, copyq menu
bind = $mainmod, v, exec, pkill fuzzel || cliphist list | fuzzel --no-fuzzy --icon-theme=candy-icons --background-color=1A1513dd --text-color=F8D4D2ff --match-color=FFB3B1ff --border-width=2 --border-radius=15 --border-color=EB8A89ff	 --selection-color=585b70ff --selection-text-color=F8D4D2ff --selection-match-color=FFB3B1ff --font="Lexend"  --prompt=">>  " --dmenu | cliphist decode | wl-copy

# Media
bind = $mainmod shift, n, exec, playerctl next || playerctl position `bc <<< "100 * $(playerctl metadata mpris:length) / 1000000 / 100"`
bind = $mainmod shift, b, exec, playerctl previous
bind = $mainmod shift, P, exec, playerctl play-pause

#Lock screen  |  blur: --effect-blur=20x20
bind = $mainmod, L, exec, ~/.local/bin/lock.sh
bind = $mainmod shift, L, exec, ~/.local/bin/lock.sh
bindl = $mainmod shift, L, exec, sleep 0.1 && systemctl suspend

# App launcher
bind = $mainmod, Slash, exec, pkill anyrun || anyrun

################################# eww keybinds ################################
# bindr = $mainmod, $mainmod_L, exec, ~/.config/eww/scripts/toggle-powerview.sh --keypress &
bindr = control $mainmod, r, exec, pkill eww && eww daemon && eww open bar && eww open bottomline
bind = control $mainmod, t, exec, cd ~/.config/eww && scripts/switchwall
bind = $mainmod, Tab, exec, ~/.config/eww/scripts/toggle-powerview.sh &
bind = $mainmod, m, exec, ~/.config/eww/scripts/toggle-bottomsheet.sh &
bind = $mainmod, b, exec, ~/.config/eww/scripts/toggle-sideleft.sh &
bind = $mainmod, o, exec, ~/.config/eww/scripts/toggle-sideleft.sh &
bind = $mainmod, n, exec, ~/.config/eww/scripts/toggle-sideright.sh &
bind = $mainmod, a, exec, ~/.config/eww/scripts/toggle-dashboard.sh &
bind = control $mainmod, mouse:273, exec, ~/.config/eww/scripts/toggle-supercontext.sh &
bind = $mainmod, Menu, exec, ~/.config/eww/scripts/toggle-supercontext.sh &

## Testing
## just test™
bind = $mainmod alt, f10, exec, notify-send "Test notification" "$(hyprctl version | head -1)"
bind = Super+Alt, f11, exec, bash -c 'RANDOM_IMAGE=$(find ~/Pictures -type f | grep -v -i "nipple" | grep -v -i "pussy" | shuf -n 1); ACTION=$(notify-send "Test notification with body image" "This notification should contain your user account <b>image</b> and <a href=\"https://discord.com/app\">Discord</a> <b>icon</b>. Oh and here is a random image in your Pictures folder: <img src=\"$RANDOM_IMAGE\" alt=\"Testing image\"/>" -a "Hyprland keybind" -p -h "string:image-path:/var/lib/AccountsService/icons/$USER" -t 6000 -i "discord" -A "openImage=Open profile image" -A "action2=Open the random image" -A "action3=Useless button"); [[ $ACTION == *openImage ]] && xdg-open "/var/lib/AccountsService/icons/$USER"; [[ $ACTION == *action2 ]] && xdg-open \"$RANDOM_IMAGE\"' # [hidden]
bind = Super+Alt, f12, exec, bash -c 'RANDOM_IMAGE=$(find ~/Pictures -type f | grep -v -i "nipple" | grep -v -i "pussy" | shuf -n 1); ACTION=$(notify-send "Test notification" "This notification should contain a random image in your <b>Pictures</b> folder and <a href=\"https://discord.com/app\">Discord</a> <b>icon</b>.\n<i>Flick right to dismiss!</i>" -a "Discord (fake)" -p -h "string:image-path:$RANDOM_IMAGE" -t 6000 -i "discord" -A "openImage=Open profile image" -A "action2=Useless button" -A "action3=Cry more"); [[ $ACTION == *openImage ]] && xdg-open "/var/lib/AccountsService/icons/$USER"' # [hidden]
bind = Super+Alt, Equal, exec, notify-send "Urgent notification" "Ah hell no" -u critical -a 'Hyprland keybind' # [hidden]


############################ Keybinds for Hyprland ############################
# Swap windows
bind = $mainmod shift, left, movewindow, l
bind = $mainmod shift, right, movewindow, r
bind = $mainmod shift, up, movewindow, u
bind = $mainmod shift, down, movewindow, d
# Move focus
bind = $mainmod, left, movefocus, l
bind = $mainmod, right, movefocus, r
bind = $mainmod, up, movefocus, u
bind = $mainmod, down, movefocus, d
bind = $mainmod, BracketLeft, movefocus, l
bind = $mainmod, BracketRight, movefocus, r

# Workspace, window, tab switch with keyboard
bind = control $mainmod, right, workspace, +1
bind = control $mainmod, left, workspace, -1
bind = control $mainmod, BracketLeft, workspace, -1
bind = control $mainmod, BracketRight, workspace, +1
bind = control $mainmod, up, workspace, -5
bind = control $mainmod, down, workspace, +5
bind = $mainmod, Page_Down, workspace, +1
bind = $mainmod, Page_Up, workspace, -1
bind = control $mainmod, Page_Down, workspace, +1
bind = control $mainmod, Page_Up, workspace, -1
bind = $mainmod shift, Page_Down, movetoworkspace, +1
bind = $mainmod shift, Page_Up, movetoworkspace, -1
bind = control $mainmod shift, Right, movetoworkspace, +1
bind = control $mainmod shift, Left, movetoworkspace, -1
bind = $mainmod shift, mouse_down, movetoworkspace, -1
bind = $mainmod shift, mouse_up, movetoworkspace, +1

# Window split ratio
binde = $mainmod, Minus, splitratio, -0.1
binde = $mainmod, Equal, splitratio, 0.1
binde = $mainmod, Semicolon, splitratio, -0.1
binde = $mainmod, Apostrophe, splitratio, 0.1

# Fullscreen
bind = $mainmod, f, fullscreen, 0
bind = $mainmod, d, fullscreen, 1
bind = $mainmod alt, f, fullscreen, 3

# Switching
bind = $mainmod, 1, workspace, 1
bind = $mainmod, 2, workspace, 2
bind = $mainmod, 3, workspace, 3
bind = $mainmod, 4, workspace, 4
bind = $mainmod, 5, workspace, 5
bind = $mainmod, 6, workspace, 6
bind = $mainmod, 7, workspace, 7
bind = $mainmod, 8, workspace, 8
bind = $mainmod, 9, workspace, 9
bind = $mainmod, 0, workspace, 10
bind = $mainmod, S, togglespecialworkspace,
bind = control $mainmod, S, togglespecialworkspace,
bind = alt, Tab, cyclenext
bind = alt, Tab, bringactivetotop,   # bring it to the top

# Move window to workspace $mainmod + alt + [0-9]
    #bind = $mainmod alt, 1, movetoworkspacesilent, 1
    #bind = $mainmod alt, 2, movetoworkspacesilent, 2
    #bind = $mainmod alt, 3, movetoworkspacesilent, 3
    #bind = $mainmod alt, 4, movetoworkspacesilent, 4
    #bind = $mainmod alt, 5, movetoworkspacesilent, 5
    #bind = $mainmod alt, 6, movetoworkspacesilent, 6
    #bind = $mainmod alt, 7, movetoworkspacesilent, 7
    #bind = $mainmod alt, 8, movetoworkspacesilent, 8
    #bind = $mainmod alt, 9, movetoworkspacesilent, 9
    #bind = $mainmod alt, 0, movetoworkspacesilent, 10
## i got used to use super + shift so... (noneisreal)
bind = $mainmod shift, 1, movetoworkspacesilent, 1
bind = $mainmod shift, 2, movetoworkspacesilent, 2
bind = $mainmod shift, 3, movetoworkspacesilent, 3
bind = $mainmod shift, 4, movetoworkspacesilent, 4
bind = $mainmod shift, 5, movetoworkspacesilent, 5
bind = $mainmod shift, 6, movetoworkspacesilent, 6
bind = $mainmod shift, 7, movetoworkspacesilent, 7
bind = $mainmod shift, 8, movetoworkspacesilent, 8
bind = $mainmod shift, 9, movetoworkspacesilent, 9
bind = $mainmod shift, 0, movetoworkspacesilent, 10

bind = control+shift $mainmod, Up, movetoworkspacesilent, special
bind = $mainmod alt, s, movetoworkspacesilent, special

# Scroll through existing workspaces with (control) + $mainmod + scroll
bind = $mainmod, mouse_up, workspace, +1
bind = $mainmod, mouse_down, workspace, -1
bind = control $mainmod, mouse_up, workspace, +1
bind = control $mainmod, mouse_down, workspace, -1

# Move/resize windows with $mainmod + LMB/RMB and dragging
bindm = $mainmod, mouse:272, movewindow
bindm = $mainmod, mouse:273, resizewindow
bindm = $mainmod, mouse:274, movewindow
bindm = $mainmod, z, movewindow
bind = control $mainmod, Backslash, resizeactive, exact 640 480
